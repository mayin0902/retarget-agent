schema_version: "1.0"
dataset_root: local_data/retarget_square_public_v2/heldout240
output_root: runs
run_id: square-public-v2-heldout240-20260812
seed: 20260812
device: cpu
methods: [direct_warp, crop, seam, mesh]
analysis:
  gradient_weight: 0.45
  contrast_weight: 0.35
  center_weight: 0.20
  region_padding_ratio: 0.02
  detector_mode: required
  model_root: models/analyzers
  face_confidence_threshold: 0.55
  object_confidence_threshold: 0.35
  object_nms_threshold: 0.50
  text_binary_threshold: 0.30
  text_polygon_threshold: 0.50
  text_max_candidates: 200
  logo_candidate_limit: 24
method_parameters:
  direct_warp: {interpolation: lanczos}
  crop: {grid_step: 48, scales: [1.0, 0.94, 0.88]}
  seam: {max_seams_per_axis: 24, protection_weight: 18.0, tolerance_weight: 3.0}
  mesh: {grid_columns: 12, grid_rows: 12, protection_gain: 1.8, minimum_cell_fraction: 0.25}
selector: {selector_id: technical_risk_v1}

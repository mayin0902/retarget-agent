# Issue tracker: GitHub

Issues and specs for this repository live in GitHub Issues at the configured `origin` remote.

- Use `gh issue view/list` for reads when issue context is requested.
- Creating, editing, labelling, commenting on, or closing an issue is an external write and requires explicit user authorization.
- Pull requests are not a triage request surface by default.


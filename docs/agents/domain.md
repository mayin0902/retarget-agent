# Domain docs

This is a single-context repository.

Before changing code, read root `CONTEXT.md` and any relevant decision in `docs/adr/`. Use the glossary terms consistently. If a proposed change contradicts an ADR, surface that conflict instead of silently replacing the decision.

